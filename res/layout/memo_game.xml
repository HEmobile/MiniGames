<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/gameContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >

	<GridView
	    android:id="@+id/gridView"
	    android:layout_margin="@dimen/memo_grid_margin"
	    android:layout_width="match_parent" 
	    android:layout_height="match_parent"
	    android:columnWidth="@dimen/card_side_size"
	    android:numColumns="auto_fit"
	    android:verticalSpacing="@dimen/memo_grid_vertical_spacing"
	    android:horizontalSpacing="@dimen/memo_grid_horizontal_spacing"
	    android:stretchMode="columnWidth"
	    android:gravity="center"
	/>
</RelativeLayout>